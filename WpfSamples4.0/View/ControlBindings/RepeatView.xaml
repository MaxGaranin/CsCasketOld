<Window x:Class="WpfSamples40.View.ControlBindings.RepeatView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfSamples40.View.ControlBindings"
        mc:Ignorable="d"
        Title="RepeatView" Height="400" Width="600"
        d:DataContext="{d:DesignInstance local:RepeatView}">
    <Grid x:Name="LayoutRoot">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        
        <TextBox Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}"/>
        
        <local:RepeatControl Grid.Row="1"
                             Repeat1="{Binding Value, Mode=TwoWay}"/>
    </Grid>
</Window>
