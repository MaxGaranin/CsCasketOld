<Window x:Class="WpfSamples40.ComplexSamples.Measures.TestMeasureView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:measures="clr-namespace:WpfSamples40.ComplexSamples.Measures"
        mc:Ignorable="d"
        Title="TestMeasureView" Height="400" Width="600"
        d:DataContext="{d:DesignInstance measures:TestMeasureView}">
    <Grid x:Name="LayoutRoot">
        <StackPanel>
            <ComboBox Width="200" HorizontalAlignment="Left"
                      ItemsSource="{Binding TempMeasures}"
                      SelectedItem="{Binding CurrentTempMeasure}" />

            <measures:TestMeasureControl 
                TempMeasureContainer="{Binding TempMeasureContainer}"/>
            
        </StackPanel>
    </Grid>
</Window>