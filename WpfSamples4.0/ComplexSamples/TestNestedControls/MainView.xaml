<Window x:Class="WpfSamples40.ComplexSamples.TestNestedControls.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:testNestedControls="clr-namespace:WpfSamples40.ComplexSamples.TestNestedControls"
        Title="WellModelView" Height="400" Width="600"
        DataContext="{Binding Source={StaticResource Locator}, Path=WellModel}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        
        <testNestedControls:WellFluidModel Grid.Row="0" x:Name="WellFluidControl"/>    
        
        <StackPanel Grid.Row="1" HorizontalAlignment="Right" Margin="5">
            <Button Content="Применить" Width="100" Height="30"
                    Command="{Binding ApplyCommand}"/>
        </StackPanel>
    </Grid>
</Window>
