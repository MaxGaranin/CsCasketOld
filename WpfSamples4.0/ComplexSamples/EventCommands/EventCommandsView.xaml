<Window x:Class="WpfSamples40.ComplexSamples.EventCommands.EventCommandsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
        xmlns:command="http://www.galasoft.ch/mvvmlight"
        Title="EventCommandsView" Height="400" Width="600">
    <Grid>
        <Canvas x:Name="drawCanvas" Background="Transparent">
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="MouseMove">
                    <command:EventToCommand Command="{Binding Path=CanvasMouseMoveCommand, Mode=OneWay}"
                                        PassEventArgsToCommand="True"/>
                </i:EventTrigger>
            </i:Interaction.Triggers>
        </Canvas>
        <TextBlock x:Name="txtCoords" Width="100" Height="20"
                   Text="{Binding Coords}"
                   HorizontalAlignment="Left" VerticalAlignment="Bottom"></TextBlock>
    </Grid>
</Window>
