<UserControl x:Class="WpfSamples40.Controls.FieldUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="20" d:DesignWidth="200">
    
    <StackPanel x:Name="LayoutRoot" Orientation="Horizontal">
        <TextBlock Text="{Binding Path=Label}" Width="100" VerticalAlignment="Center"/>
        <TextBox Text="{Binding Path=Value}" Width="100"/>
        
<!--        <TextBlock Text="{x:Static view:FieldUserControl.TEST}" Width="100"></TextBlock>-->
        
        <TextBlock Text="{Binding Path=ParentDataContext.ZipCode}" Margin="10,0,0,0"/>

<!--        <TextBlock Margin="10,0,0,0"-->
<!--                   Text="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}, -->
<!--                                  Path=DataContext.ZipCode}"/>-->
        
    </StackPanel>
</UserControl>
