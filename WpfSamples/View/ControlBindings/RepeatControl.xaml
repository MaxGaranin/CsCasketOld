<UserControl x:Class="WpfSamples.View.ControlBindings.RepeatControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             d:DataContext="{d:DesignInstance local:RepeatControl}">
    <Grid x:Name="LayoutRoot">
        <StackPanel Orientation="Horizontal">
            <TextBox Margin="5"
                     Text="{Binding Repeat1, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBox Margin="5"
                     Text="{Binding Repeat2, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>        
    </Grid>
</UserControl>
